# --- Database (Neon Postgres) ---
# Get this from https://console.neon.tech → your project → Connection string (pooled)
DATABASE_URL="postgresql://user:password@ep-xxx-pooler.region.aws.neon.tech/neondb?sslmode=require"

# --- Better Auth ---
# Generate a strong secret: `openssl rand -base64 32`
BETTER_AUTH_SECRET="replace-with-a-long-random-string"
BETTER_AUTH_URL="http://localhost:3000"

# --- Vercel Blob (private attachments / proof of payment) ---
# Create a Blob store in the Vercel dashboard → Storage → Blob, then copy the token.
BLOB_READ_WRITE_TOKEN="vercel_blob_rw_xxxxxxxx"

# --- Cron protection ---
# Any long random string. Vercel Cron sends it as a Bearer token to /api/cron/*.
CRON_SECRET="replace-with-a-long-random-string"

# --- Public app URL (used in emails / links, Phase 2) ---
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# --- Email (Phase 2, OPTIONAL) ---
# Dev with nothing set = a free Ethereal test inbox with a preview link.
#
# Recommended for real delivery: Resend (free 3,000/mo, no SMTP).
#   1. Create a free account at https://resend.com
#   2. API Keys -> create one -> paste below
#   3. EMAIL_FROM: onboarding@resend.dev works for testing to your own address;
#      verify a domain to email arbitrary customers.
RESEND_API_KEY=""
EMAIL_FROM="InvoiceBook <onboarding@resend.dev>"

# Optional SMTP fallback (only if you prefer SMTP over Resend):
# SMTP_HOST=""
# SMTP_PORT="587"
# SMTP_USER=""
# SMTP_PASS=""
